# 배열의 평균값

def solution(numbers):
    answer = 0
    size = len(numbers)
    res = sum(numbers)/size
    print(res)
    
    if nam == 0 :
        answer = res
    elif nam == 5 :
        answer = res
    else :
        answer = round(res,1)
    
    return answer
